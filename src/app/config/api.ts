// API Configuration
// This application uses Firebase for backend services
// Authentication and Firestore database are configured in src/app/config/firebase.ts

// Legacy API_BASE_URL is no longer used - kept for backward compatibility
export const API_BASE_URL = '';
